# 将 SoloSpec 升级为当前系统行为说明

状态：completed

## 目标

将 SoloSpec 的规范定位从仅记录稳定约束，升级为说明当前有效系统行为与必要约束；保留轻量任务和 ADR 流程，使读者可主要依靠文档理解功能，无需先阅读源码。

## 方案

更新项目规则、规范入口、任务模板、ADR 技能和中英文接入说明；新增按需使用的功能说明模板。明确已有项目通过合并升级，模板变更不会自动传播。

## 验收标准

- `AGENTS.md` 和 `docs/spec/index.md` 明确当前行为变化无需等待稳定即可同步规范。
- 规范要求直接写出当前结论，不能只链接 ADR、任务记录或源码。
- 功能说明模板覆盖读者理解行为所需的核心信息，且不要求机械填写。
- 小需求频繁变更直接维护同一份规范；只有重要长期取舍建立 ADR。
- 两份 README 说明已有项目需要合并升级，模板更新不会自动同步。

## 进度与下一步

- 已完成：更新规则、模板、ADR 技能和中英文说明，并新增功能说明模板。
- 验证：Markdown 本地相对链接均可解析；`git diff --check` 通过；已检查规则、模板和中英文说明均覆盖本任务的验收标准。
- 交付：独立功能 worktree 编辑后同步回原 SoloSpec 目录，保留原分支，不提交、不推送；已接入项目仍需显式合并，本次同步到 hire-agent。
- 下一步：none。

## Related ADRs

none
