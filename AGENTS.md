# SoloSpec 项目约定

本文件定义项目的默认工作方式。具体业务和技术约束从 `docs/spec/index.md` 查找，长期决策从 `docs/adr/index.md` 查找。用户明确要求优先于这些默认约定。

## 四步工作方式

1. **读取相关约束**：按本次影响范围，从规范索引读取相关文档；涉及长期设计取舍时，查阅 ADR 索引及相关有效决策。不要默认读取全部历史。
2. **按需留下记录**：简单任务直接处理。需要跨会话继续、包含多个有依赖的实施阶段，或用户要求保留计划时，使用任务记录。仅在缺失信息会实质改变结果时澄清；不设置固定需求访谈或阶段审批。
3. **实现并验证**：遵循受影响模块的质量要求，按行为变化和回归风险选择检查。是否使用子 Agent 取决于独立子任务的实际收益，不要求固定角色流水线。
4. **交付并沉淀**：说明完成内容、验证结果、剩余问题。长期决策写入 ADR，稳定约束写入规范，未完成工作写入任务记录；没有新知识就不增加文档。不强制会话日志、复盘、归档或 Git 提交。

## 任务记录

- 创建前先检查 `docs/tasks/` 是否已有对应任务。新记录放在 `docs/tasks/YYYY-MM-DD-<topic>/task.md`，使用本地创建日期和小写英文短横线主题；格式见 `docs/templates/task.md`。
- 同一任务继续使用原目录。不同任务遇到同名路径时，增加能区分目标的主题后缀，不覆盖既有内容。
- 文首状态为 `in_progress`、`blocked` 或 `completed`，正文记录目标、方案、验收标准、进度与下一步。相关 ADR 用相对链接引用，不重复决策正文。
- 续接优先使用用户指定或当前对话明确的路径；否则按目标与未完成事项检查现有记录，不能仅凭修改时间选择。多个候选无法区分时询问一次，同时继续不依赖选择的工作。
- 方案实质变化、阶段完成、遇到阻碍或结束未完成会话时更新记录。保留下一步所需的文件、验证结果和阻碍。完成后原地标记 `completed`，不维护额外状态数据库或任务索引。
- 已有项目采用本模板时，允许旧任务沿用原位置和格式；将旧记录入口补充到 `docs/tasks/README.md`，不为统一格式批量迁移。

## 决策与规范

- ADR 的唯一目录是 `docs/adr/`。先搜索有效决策，再按 `template.md` 分配尚未使用的四位编号；在 `index.md` 添加条目。
- 仅对影响长期维护的取舍建立 ADR，例如模块边界、数据契约、关键依赖或运维策略。局部实现细节和已有决策覆盖的选择不重复记录。
- 状态使用 `proposed`、`accepted`、`rejected`、`superseded`、`deprecated`。已有明确授权可以支持接受决策，不重复审批；尚有用户待定取舍时保持 `proposed`。
- 替代已接受决策时新建 ADR，更新双方状态与关系，保留旧正文。项目级 ADR 可以没有任务记录。
- ADR 说明为什么，任务记录说明本次如何推进，规范说明以后必须遵守什么。只有产生稳定约束时才更新对应规范并维护引用，不要求三份文档同时创建。

## 修改与检查边界

- 解释和评审不默认修改文件；执行只处理已授权范围，保留无关工作。
- 检查应暴露真实失败，不通过假成功、吞错或无依据的限制换取通过。交付时区分实际验证、静态检查和未验证内容。
- 不把凭证、个人数据或真实运行数据写入模板、文档或测试样本。删除既有内容优先使用可恢复方式。
- 人类阅读的文档默认使用中文；路径、代码、字段和状态值使用英文。引入项目后可统一调整文档语言。
